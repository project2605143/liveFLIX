'use client'

import { useState } from 'react'
import { ArrowRight, Check, Globe2, Play, Search, ShieldCheck, Sparkles, X, Zap } from 'lucide-react'
import StreamlineDashboard from '@/components/streamline-dashboard'

function Logo() {
  return <div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-xl bg-teal-400 text-zinc-950"><Play className="size-4 fill-current" /></div><div><p className="font-semibold tracking-tight text-zinc-100">Streamline</p><p className="text-[10px] uppercase tracking-[0.24em] text-zinc-500">Live TV, organized.</p></div></div>
}

function AuthModal({ mode, onClose, onSuccess, onMode }: { mode: 'sign-in' | 'sign-up'; onClose: () => void; onSuccess: () => void; onMode: (mode: 'sign-in' | 'sign-up') => void }) {
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [name, setName] = useState('')
  const valid = email.includes('@') && password.length >= 6 && (mode === 'sign-in' || name.trim().length > 1)
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-zinc-950/85 p-4 backdrop-blur-md"><div className="relative w-full max-w-md rounded-3xl border border-zinc-800 bg-zinc-900 p-7 shadow-2xl shadow-black/40"><button onClick={onClose} className="absolute right-5 top-5 rounded-full p-2 text-zinc-500 hover:bg-zinc-800 hover:text-zinc-200" aria-label="Close sign in"><X className="size-4" /></button><div className="mb-8"><Logo /><p className="mt-8 text-xs font-semibold uppercase tracking-[0.22em] text-teal-300">{mode === 'sign-in' ? 'Welcome back' : 'Start watching'}</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-zinc-100">{mode === 'sign-in' ? 'Sign in to Streamline' : 'Create your Streamline account'}</h2><p className="mt-3 text-sm leading-6 text-zinc-400">Your channels, favorites, and viewing setup stay in sync wherever you watch.</p></div><form onSubmit={(event) => { event.preventDefault(); if (valid) onSuccess() }} className="grid gap-4">{mode === 'sign-up' && <label className="grid gap-2 text-xs font-medium text-zinc-400">Name<input value={name} onChange={(event) => setName(event.target.value)} className="rounded-xl border border-zinc-700 bg-zinc-950 px-3.5 py-3 text-sm text-zinc-100 outline-none ring-teal-400/30 placeholder:text-zinc-600 focus:ring-4" placeholder="Aarav Mehta" /></label>}<label className="grid gap-2 text-xs font-medium text-zinc-400">Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="rounded-xl border border-zinc-700 bg-zinc-950 px-3.5 py-3 text-sm text-zinc-100 outline-none ring-teal-400/30 placeholder:text-zinc-600 focus:ring-4" placeholder="you@example.com" /></label><label className="grid gap-2 text-xs font-medium text-zinc-400">Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="rounded-xl border border-zinc-700 bg-zinc-950 px-3.5 py-3 text-sm text-zinc-100 outline-none ring-teal-400/30 placeholder:text-zinc-600 focus:ring-4" placeholder="At least 6 characters" /></label><button disabled={!valid} className="mt-2 flex items-center justify-center gap-2 rounded-xl bg-teal-400 px-4 py-3.5 text-sm font-semibold text-zinc-950 transition hover:bg-teal-300 disabled:cursor-not-allowed disabled:opacity-40">{mode === 'sign-in' ? 'Enter Streamline' : 'Create account'}<ArrowRight className="size-4" /></button></form><p className="mt-6 text-center text-xs text-zinc-500">{mode === 'sign-in' ? "Don't have an account?" : 'Already have an account?'} <button onClick={() => onMode(mode === 'sign-in' ? 'sign-up' : 'sign-in')} className="font-medium text-teal-300 hover:text-teal-200">{mode === 'sign-in' ? 'Sign up' : 'Sign in'}</button></p></div></div>
}

export default function StreamlineLanding() {
  const [auth, setAuth] = useState<'sign-in' | 'sign-up' | null>(null)
  const [entered, setEntered] = useState(false)
  if (entered) return <StreamlineDashboard />
  return <main className="min-h-screen overflow-hidden bg-zinc-950 text-zinc-100"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-10"><Logo /><nav className="hidden items-center gap-8 text-sm text-zinc-500 md:flex"><a href="#why" className="hover:text-zinc-200">Why Streamline</a><a href="#how" className="hover:text-zinc-200">How it works</a><a href="#access" className="hover:text-zinc-200">Access</a></nav><div className="flex items-center gap-3"><button onClick={() => setAuth('sign-in')} className="hidden rounded-xl px-4 py-2.5 text-sm text-zinc-400 hover:text-zinc-100 sm:block">Sign in</button><button onClick={() => setAuth('sign-up')} className="rounded-xl bg-zinc-100 px-4 py-2.5 text-sm font-semibold text-zinc-950 hover:bg-white">Get started</button></div></header><section className="relative mx-auto max-w-7xl px-6 pb-24 pt-20 lg:px-10 lg:pb-32 lg:pt-32"><div className="pointer-events-none absolute right-0 top-8 hidden h-[520px] w-[520px] rounded-full bg-teal-400/10 blur-[120px] lg:block" /><div className="relative grid items-center gap-16 lg:grid-cols-[1.05fr_.95fr] lg:gap-12"><div className="max-w-2xl"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-teal-400/20 bg-teal-400/5 px-3 py-1.5 text-xs font-medium text-teal-300"><Sparkles className="size-3.5" />The quieter way to watch live TV</div><h1 className="text-balance text-5xl font-semibold leading-[1.03] tracking-[-0.05em] text-zinc-100 sm:text-6xl lg:text-8xl">Every channel.<br /><span className="text-teal-300">One clear place.</span></h1><p className="mt-7 max-w-xl text-pretty text-lg leading-8 text-zinc-400">Streamline brings live television from around the world into a focused, beautiful guide built for finding what you actually want to watch.</p><div className="mt-9 flex flex-wrap items-center gap-3"><button onClick={() => setAuth('sign-up')} className="flex items-center gap-2 rounded-xl bg-teal-400 px-5 py-3.5 text-sm font-semibold text-zinc-950 hover:bg-teal-300">Explore Streamline <ArrowRight className="size-4" /></button><button onClick={() => setAuth('sign-in')} className="rounded-xl border border-zinc-700 px-5 py-3.5 text-sm font-medium text-zinc-300 hover:border-zinc-500 hover:text-zinc-100">I already have an account</button></div><div className="mt-8 flex items-center gap-5 text-xs text-zinc-500"><span className="flex items-center gap-2"><Check className="size-3.5 text-teal-400" />No cluttered guides</span><span className="flex items-center gap-2"><Check className="size-3.5 text-teal-400" />Personal favorites</span></div></div><div className="relative"><div className="overflow-hidden rounded-3xl border border-zinc-800 bg-zinc-900 p-3 shadow-2xl shadow-black/50"><div className="rounded-2xl border border-zinc-800/80 bg-zinc-950 p-4"><div className="flex items-center justify-between border-b border-zinc-800 pb-4"><div className="flex items-center gap-2"><div className="size-2 rounded-full bg-teal-400" /><span className="text-xs font-medium text-zinc-300">Your streaming dashboard</span></div><Search className="size-4 text-zinc-600" /></div><div className="grid grid-cols-2 gap-3 pt-4 sm:grid-cols-3"><div className="col-span-2 row-span-2 rounded-2xl bg-gradient-to-br from-teal-400/30 via-zinc-800 to-zinc-900 p-5 sm:col-span-2"><div className="flex h-40 flex-col justify-end"><span className="text-[10px] uppercase tracking-[0.2em] text-teal-200">Featured now</span><p className="mt-2 text-2xl font-semibold text-white">Find your signal.</p><p className="mt-1 text-xs text-zinc-400">Live channels, without the noise.</p></div></div><div className="rounded-2xl bg-zinc-800 p-4"><Globe2 className="size-5 text-teal-300" /><p className="mt-10 text-xs text-zinc-400">70+ countries</p></div><div className="rounded-2xl bg-zinc-800 p-4"><Zap className="size-5 text-teal-300" /><p className="mt-10 text-xs text-zinc-400">Live status</p></div></div></div></div><div className="absolute -bottom-5 -left-5 hidden items-center gap-3 rounded-2xl border border-zinc-700 bg-zinc-900 px-4 py-3 shadow-xl sm:flex"><span className="flex size-9 items-center justify-center rounded-xl bg-teal-400/15 text-teal-300"><ShieldCheck className="size-4" /></span><div><p className="text-xs font-medium text-zinc-200">Your setup, synced</p><p className="text-[11px] text-zinc-500">Favorites follow you everywhere</p></div></div></div></div></section><section id="why" className="border-y border-zinc-800/80 bg-zinc-900/30"><div className="mx-auto grid max-w-7xl gap-px bg-zinc-800/70 md:grid-cols-3">{[{ icon: Search, title: 'Find faster', text: 'Search through a clean, human-friendly channel library instead of fighting a guide.' }, { icon: Globe2, title: 'See more of the world', text: 'Explore news, culture, and live moments from India and beyond.' }, { icon: ShieldCheck, title: 'Keep your signal', text: 'Save favorites and return to a setup that feels like yours.' }].map(({ icon: Icon, title, text }) => <div key={title} className="bg-zinc-950 px-7 py-10 lg:px-10"><Icon className="size-5 text-teal-300" /><h2 className="mt-8 text-lg font-medium text-zinc-100">{title}</h2><p className="mt-3 max-w-xs text-sm leading-6 text-zinc-500">{text}</p></div>)}</div></section><section id="how" className="mx-auto max-w-7xl px-6 py-24 lg:px-10"><div className="max-w-xl"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-teal-300">A better starting point</p><h2 className="mt-4 text-4xl font-semibold tracking-tight text-zinc-100 sm:text-5xl">Open the app.<br />Know where to go.</h2><p className="mt-5 text-base leading-7 text-zinc-400">Create your account, pick a signal, and build your own compact library of live TV. Streamline keeps the experience intentional from the first click.</p></div><div className="mt-14 grid gap-4 md:grid-cols-3">{['Create your space', 'Explore the signal', 'Make it yours'].map((step, index) => <div key={step} className="border-t border-zinc-700 pt-5"><p className="font-mono text-xs text-teal-300">0{index + 1}</p><h3 className="mt-7 text-xl font-medium text-zinc-100">{step}</h3><p className="mt-3 text-sm leading-6 text-zinc-500">{['Sign in once and keep your viewing setup ready across sessions.', 'Browse by country, genre, language, or what is live right now.', 'Save the channels that matter and make your home screen yours.'][index]}</p></div>)}</div></section><section id="access" className="mx-6 mb-6 rounded-3xl bg-teal-400 px-6 py-16 text-zinc-950 sm:px-10 lg:mx-10 lg:px-16"><div className="flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.22em] text-teal-950/70">Your next channel is waiting</p><h2 className="mt-4 max-w-2xl text-4xl font-semibold tracking-tight sm:text-5xl">Make live TV feel simple again.</h2></div><button onClick={() => setAuth('sign-up')} className="flex w-fit items-center gap-2 rounded-xl bg-zinc-950 px-5 py-3.5 text-sm font-semibold text-zinc-100 hover:bg-zinc-800">Get started free <ArrowRight className="size-4" /></button></div></section><footer className="mx-auto flex max-w-7xl flex-col gap-4 px-6 py-8 text-xs text-zinc-600 sm:flex-row sm:items-center sm:justify-between lg:px-10"><Logo /><span>© 2026 Streamline. Live TV, organized.</span></footer>{auth && <AuthModal mode={auth} onClose={() => setAuth(null)} onMode={setAuth} onSuccess={() => { setAuth(null); setEntered(true) }} />}</main>
}
